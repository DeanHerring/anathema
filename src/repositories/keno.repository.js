// import { db } from '../db/db.js';

// const isExistPlayer = async (playerId) => {
//   const sql = `SELECT * FROM users WHERE id = ${playerId}`;
//   const [rows] = await db.query(sql);

//   return rows;
// };

// export const KenoRepository = {
//   isExistPlayer,
// };
